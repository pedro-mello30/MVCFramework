var
  tmp = require('tmp'),
  spawn = require('tmp/test/spawn');

var graceful = spawn.arg;

if (graceful) {
  tmp.setGracefulCleanup();
}

spawn.tmpFunction(function (err, name) {
  spawn.out(name, function () {
    throw new Error("Thrown on purpose");
  });
});
